# Conch GUI 工作流进度表(原 R-Shell）

> 用途:断点续作。对话中断后,先读本表 + [选型对比](01-GUI技术选型对比.md) + [架构设计](02-架构设计.md) + [Stage4 SFTP](06-Stage4-SFTP.md) + [Stage5 监控](07-Stage5-监控.md) + [Stage3.6 终端增强](08-Stage3.6-终端增强.md) 恢复上下文,从断点继续。
> 最后更新:2026-06-23(追加:**品牌化 Conch + 新图标 + 去 OpenSSL(纯 Rust 加密)+ 清空默认连接 + macOS/Windows 出包〔自带 VC++ 运行时〕+ NSIS 桌面安装器 + 仓库改名 `conch` 并发 GitHub Release**;前序:命令块支持 Windows/PowerShell,见文末时间线)

## 一句话现状

**已选定 Flutter**,Stage 0 / 1 / 2 / **3 均已完成且全绿**。**Stage 3:多标签 PTY 终端已在 macOS 跑通**——core 新增「按 pty_id 的多 PTY」能力(可同主机多标签并存,旧的 connection_id 单 PTY 接口保留给 CLI,零回归);bridge 暴露 `session_open/close`、`pty_open/subscribe/write/resize/close`,PTY 输出经 `StreamSink<Vec<u8>>` 推给 Dart;Flutter 接入 `xterm` 4.0.0,实现多标签终端(每标签一个 `TerminalSession`:连接→开 PTY→订阅字节流→渲染;输入/resize 回写;增量 UTF-8 解码防乱码;断开后可重连),双击连接或菜单「打开终端」开新标签,关闭标签即关 PTY、该连接最后一个标签关闭即断会话。`cargo build --workspace` 零警告 / `cargo test` 32 通过、`flutter analyze` 0 告警、`flutter build macos` ✓ Built、`flutter run -d macos` 启动无异常。**2026-06-22 追加:真机端到端验证通过**——发现并修复 **macOS 沙箱致命阻断**(`macos/Runner/*.entitlements` 开着 `app-sandbox` 且缺 `network.client`,GUI 根本无法对外建立 SSH 连接、也读不到 `~/.ssh` 私钥/known_hosts)→ 关闭 `app-sandbox` + 补 `network.client/server`;在 GUI 里连到局域网真实 Windows 开发机(`Win-Dev (shanh)` = `shanh@192.168.0.112:22`,publickey `~/.ssh/pgid_win`)成功开出 **PowerShell PTY**,交互跑 `whoami / hostname / (Get-CimInstance Win32_OperatingSystem).Caption` 拿到真实数据(`shanhaimagegojo\shanh` / `ShanhaiMageGojo` / `Microsoft Windows 11 专业工作站版 Insider Preview`),中文渲染正常。**2026-06-22 再追加:Stage 4 SFTP 双栏文件管理落地(macOS 编译验证通过)**——core 新增**基于 SFTP `read_dir` 的结构化列目录**(跨平台,含 Windows OpenSSH,不再依赖 `ls -la`)+ `realpath`(取 home / 解析 `..`)+ **带进度回调的分块上传 / 下载**(32KB,`FnMut(u64,u64)`,core 不依赖 frb);bridge 把进程级 `manager()` 上提共享 + 新增 `ensure_session`(终端 / SFTP 复用同一活跃会话),`api/sftp.rs` 暴露 `sftp_list/home/realpath/upload/download`(进度经 `StreamSink<TransferProgress>` 推送 + bridge 端 128KB 节流);Flutter 新增 `features/files/`(SftpRepository、LocalFs〔`dart:io` 浏览本地〕、TransferQueue、FilesController、FilesPanel)实现**左远程 / 右本地双栏 + 中间 ←/→ 传输 + `desktop_drop` 拖拽上传 + 底部传输队列**(进度条 / 完成 / 失败 / 清除已完成);连接树「⋯」加「打开文件」、导航第 2 项与状态栏接入。`cargo build --workspace` 零警告 / `cargo test` 32 通过 / `flutter analyze` 0 告警 / `flutter test` 6 通过 / `flutter build macos` ✓ Built。**真机端到端传输验证待下次连真实服务器补**。**2026-06-22 再追加:Stage 5 监控仪表盘落地(macOS 编译验证通过)**——监控侧栏从占位(`—` + 「Stage 5 接入」)接成**实时数据**:**core 零改动**(早有完整且有单测的 `monitor.rs`:`stats_command`/`parse_snapshot`/`SystemStats::from_samples` + `native_backend::fetch_system_stats`,CLI `stats` 在用);新增 bridge `api/monitor.rs` —— `SystemStatsDto`(与 core `SystemStats` 一一映射)+ `stats_stream(connection_id, interval_ms, sink)`:`ensure_session` 复用共享会话后循环「抓快照→`parse_snapshot`→与上帧 `from_samples` 差分(`Instant` 实测 elapsed)→`StreamSink` 推一帧→sleep」,首帧 `previous=None`(CPU%/网速为 0,内存/磁盘/负载/uptime/OS 立即可用),Dart 取消即 `sink.add` 失败退出、会话失败 `Err`→`onError`,**不主动关会话**;bridge crate 加 `tokio`(用 `time::sleep`)、`mod.rs` 注册、frb codegen 重生成绑定。Flutter 新增 `features/monitor/`(StatsRepository 薄封装、MonitorController〔跟随选中连接、四条环形历史供走势图、`idle/connecting/live/error` 状态机 + 3s 自动重试、默认 2s 采样〕、monitor_format〔对照 Rust 版的 Dart 格式化 + 单测〕、MonitorPanel〔`fl_chart` 极简折线 + 渐变填充,CPU 面积图 / 内存·磁盘进度条 / 网络双线,>80% warning、>92% danger 阈值着色,状态药丸 + 空闲/采集/出错横幅〕);`app_scaffold` 持有 `MonitorController`、`_syncMonitor()` 在每个选中入口跟随、右栏换成 `MonitorPanel`(删除内联占位 `_monitorPanel`/`_monitorCard`);`flutter pub add fl_chart`(1.2.0)。`cargo build --workspace` 零警告 / `cargo test` 32 通过 / `flutter analyze` 0 告警 / `flutter test` 11 通过(原 6 + 新增 monitor_format 5)/ `flutter build macos` ✓ Built(82.6MB)。**关键修复(frb 流错误通道)**:首版 `stats_stream` 返回 `Result<(),String>`,但 frb 对 `async fn(sink)->Result` 把**函数返回的 `Err` 放进一个 unawaited Future**(数据才走 `sink.stream`),Dart `onError` 收不到 → 连不可达主机时控制台每隔几秒刷一条 **Unhandled Exception**;改为 **`stats_stream` 永不外抛(返回 `()`)、失败封装成 `StatsFrame{sample?,error?}` 走 sink + Rust 内部自重试**(数据枚举需 `freezed`,为省依赖用 `Option` 字段 struct),Dart `onData` 分支处理。**`flutter run -d macos` 真机冒烟**:连真实 Windows 机(`shanh@192.168.0.112`),面板优雅显示「监控已断开 · Command failed with code: Some(1)」(Windows 无 `/proc`/`df`)+ 重试,**控制台 0 条 Unhandled Exception**(修复前刷屏)。**注:指标命令 Linux 专用,连非 Linux 主机命令非零退出 → 显示『监控已断开』错误态(优雅降级);Linux 真机 live 数据/图表待连可达服务器补**。**2026-06-22 再追加:Stage 6 MCP 面板 + 设置页落地(macOS 真机冒烟通过)**——补齐设计稿第 4 张 + 两个死导航入口:**bridge 新增 `api/mcp.rs`(不改 core)**——`mcp_endpoint/port/status`(sync)+ `mcp_tools`(sync,12 条静态工具目录镜像 core `RShellMcpServer`)+ `mcp_start/stop`(async:`tokio::spawn(start_mcp_server)` 存 JoinHandle、abort 释放端口,运行态/uptime 由 bridge 静态 `Mutex<McpState>` 自管,**不依赖 core `MCP_SERVER_RUNNING`**;start 后 250ms 探测秒退判定端口占用)、frb codegen 重生成;Flutter 新增 `features/mcp/`(McpController〔ChangeNotifier:读状态 + 起停 + 1s 刷新运行时长 + 一次性载入工具目录〕、McpPanel〔标题 + 状态卡:自绘 ON/OFF 大开关 + 运行中/已停止 + 监听地址/端口/运行时长 + 启动·停止·重启 + 端点只读字段含一键复制;可用工具卡:分类图标 + 工具名(等宽)+ 中文说明 + 行分隔〕)+ `features/settings/`(SettingsController〔内存态〕、SettingsPanel〔外观:主题深色/跟随系统 + 强调色;终端:字号滑杆 + 光标样式;监控:采样间隔 1/2/5s;MCP:随应用自动启动;关于:版本 v2.1.0 + 检查更新 + 复制项目地址〕);`app_scaffold` 接线:导航 MCP=4→McpPanel、设置=90→SettingsPanel、侧栏齿轮跳设置,**监控侧栏仅在 连接/终端/监控 显示**,文件/MCP/设置让页面占满(对照设计稿)。`cargo build --workspace` 零警告 / `cargo test` 32 通过 / `flutter analyze` 0 告警 / `flutter test` 38 通过 / `flutter build macos` ✓ Built。**真机冒烟(`flutter run -d macos`)**:点 MCP 进面板→点「启动」→**端口 9123 实测 LISTEN(本 app 进程)**、UI 转「运行中」+ 运行时长逐秒走、[停止]/[重启] 出现;点设置进设置页控件可交互。截图存 `docs/gui/screenshots/{mcp_panel,mcp_panel_off,settings_panel}.png`。**留到功能阶段**:MCP「已连接客户端数」实时统计、设置项持久化 + 实际生效(采样间隔/字号/自启/主题)、既有技术债(`sftp_*` frb 流错误通道、终端密码框)。下一步 **Stage 7:设置实际生效 + 持久化 + 打包(DMG/NSIS/AppImage)+ GUI CI**。**2026-06-22 再追加:Stage 8 ADB 通道编码完成(方案 B,真机授权后验证)**——core 抽出后端无关 `PtySession`(`core/pty.rs`,去 russh `channel_id`)、新增 `adb.rs`(`AdbClient`:exec / 交互 PTY〔`adb shell -t -t`〕/ push-pull / `ls` 列目录 / `exec-out cat` / 连接后强制开发者模式)、`native_backend` 引入 `Backend{Ssh,Adb}` enum 统一分派(注册表改 `Arc<RwLock<Backend>>`,exec/PTY/SFTP/监控一次 match 转发)、`model.normalized` 放行 ADB(原**强制只留 SSH**)、`connections` 加 `protocol`(ADB 放宽 username/auth 校验);bridge `ensure_session` 按协议分派(ADB→`create_adb_connection(host:port)`)、`ConnectionDto/Input` 加 `protocol` + frb 重生成;Flutter 连接编辑加「协议」选择(ADB 只填设备地址 + 端口,默认 5555,22↔5555 智能切换)、连接树 ADB 绿色角标 + `adb · host:port` 副标题;**监控每秒刷新**(`MonitorController.intervalMs=1000`)复用 Linux `/proc`+`df` 采集(安卓是 Linux,零新增采集器)。`cargo build` 零警告 / `cargo test` **37 通过**(+2 ADB 解析)/ `flutter analyze` 0 告警。**2026-06-23 真机端到端验证通过**——设备 Redmi `220233L2C` / Android 11 / `armeabi-v7a`,经无线调试**配对码配对**授权后 `5555` 与无线调试口均 `device`;用 `core/examples/adb_smoke.rs`(走 `NativeConnectionManager` 公开 API = GUI bridge 同一套)端到端跑通**命令执行(管道/分号/中文)/ 列目录 / realpath / 读写文件 / 上传下载往返(字节一致)/ 监控(OS·CPU·内存·磁盘·网速)/ 交互 PTY(捕获 `PTY_MARK_42`)** 全绿。**发现并修复 `/sdcard` 软链接目录列空 bug**(`/sdcard` 是软链接,`ls -la /sdcard` 只列链接自身→ `AdbClient::list_dir` 补尾斜杠强制列内容,`/sdcard` 由 1 项→128 项;不用 `-L` 以免破坏子项 `is_symlink` 判定),`cargo test -p r-shell-core` 仍 37 通过。**root 持久化走不通**(零售 MIUI,`adb root` = `adbd cannot run as root in production builds`),改用免 root 方案(无线调试配对的密钥信任重启后仍在,网络 adb 监听不持久、重启后一行 `adb tcpip 5555` 恢复)。workspace 已修正安卓条目为 `protocol=ADB`(`192.168.0.101:5555`)。详见 [10-Stage8-ADB](10-Stage8-ADB.md)。**2026-06-23 再追加:文件管理器交互增强(SFTP/ADB 双栏通用,编译 + 单测验证通过)**——在既有双栏 + 拖拽上传基础上补齐**完整文件操作**:① **core** 已有 `Backend{Ssh,Adb}` 统一分派的 `delete_path`/`rename_path`/`download_file_to_memory`/`upload_file_from_bytes`(SSH 走 russh SFTP、ADB 走 `rm -rf`/`mv`/`exec-out cat`/临时文件 push),对两种后端透明;② **bridge** `api/sftp.rs` 暴露 `sftp_delete`/`sftp_rename`/`sftp_read_file`(→`Vec<u8>`)/`sftp_write_file`(`Vec<u8>`→),frb 重生成 `sftpDelete/Rename/ReadFile/WriteFile`;③ **Flutter** `SftpRepository`+`LocalFs`(`dart:io`,新增 `delete`/`rename`/`readFile`/`writeFile`/`copyFileToClipboard`〔macOS osascript / Windows PowerShell Set-Clipboard〕/`tempDir`)对称封装远程 / 本地;`FilesController` 新增 `deleteRemote/renameRemote/readRemoteBytes/writeRemoteBytes/copyRemoteToClipboard` 及本地对偶(约定返回 `String?`:null=成功);`files_panel.dart` 加**右键菜单**(远程:打开编辑 / 下载 / 复制到剪贴板 / 重命名 / 删除;本地:打开编辑 / 上传 / 复制 / 重命名 / 删除)+ **内置文本编辑器**(`_openEditor`:拒绝含 NUL 的二进制 / 非 UTF-8,`_EditorDialog` 等宽全屏编辑→保存回写)+ 重命名对话框 + 删除二次确认 + **跨栏拖拽**(远程文件 `Draggable`→本地栏 `DragTarget` 即下载、本地文件→远程栏即上传,目录暂不拖拽)。验收:`cargo build --workspace` 零警告 / `cargo test` **39 通过** / `flutter analyze` **0 问题** / `flutter test` **38 通过**。**局限**:目录递归传输 / 多选批量 / 新建目录 / 断点续传仍待补;`sftp_*` 流错误通道技术债未动。**2026-06-23 再追加:品牌化 Conch + 跨平台出包 + 上线 GitHub**——① **改名 `Conch`**(扣 shell+海螺,贴合毛玻璃/珍珠母 UI):新图标(`scripts/gen_icons.py` 从单张母图扇出 macOS appiconset 七尺寸 + Windows 多尺寸 `.ico`),改全部**用户可见显示名**(app 标题 / 侧栏 / 状态栏 / 设置版本、macOS `PRODUCT_NAME`→`Conch.app`、Windows `BINARY_NAME`/窗口标题/`Runner.rc`→`Conch.exe`、Linux 标题/`APPLICATION_ID`、设置页仓库链接→新库),**内部包名 `r_shell_desktop`/frb/rust_builder 不动**(避免牵连重生成)。② **去 OpenSSL → 纯 Rust 加密**:`russh`/`russh-keys` 去掉 `openssl`+`vendored-openssl` 特性(Windows 上 vendored 触发 `openssl-src` 源码编译,需 perl/nasm 且 `nmake` 编 9 分钟仍失败,又慢又脆),改用 russh 默认纯 Rust 后端;`openssl-sys` 从依赖树彻底消失,真机 smoke(连 `shanh@192.168.0.112` 跑 `ls`)验证 **SSH 连接 / 密钥认证 / PowerShell exec 全部正常**,Windows 编译随之 13s 增量过。③ **清空默认连接**:`PersistedWorkspace::default()` 由预置 demo 主机(`root@192.168.0.103`)改为**空连接**——发布版 / 新装首启连接列表为空,绝不外泄私有连接;修掉依赖旧默认的单测。④ **跨平台出包(零环境交付)**:macOS 本机编 `Conch.app`(80.4MB)→ `dist/Conch-macos.zip`;Windows 远程在真机(私钥 `pgid_win` + 默认 PowerShell,经 `winrun.sh` EncodedCommand 驱动)编 `Conch.exe`,`scripts/build_win.ps1` 自动**拷 VC++ 运行时 7 件套**(`msvcp140*`/`vcruntime140*`/`concrt140`/`vccorlib140`,优先 VS Redist 递归定位、兜底 System32)到 Release → 双击即用,产 `dist/Conch-windows-x64.zip` + NSIS 安装器 `dist/Conch-windows-x64-setup.exe`(`packaging/windows/conch-setup.nsi`:装到 Program Files\Conch + 开始菜单/桌面快捷方式 + 卸载项,整包含运行时)。⑤ **上线 GitHub**:仓库 `MageGojo/r-shell-cli`→**`conch`**(`gh repo rename`),本地目录同步改名,首次提交从未入库的桌面 GUI + core 重构,README / README.zh-CN 全量 rebrand 到 Conch(大小写敏感替换,**保留小写 `r-shell` CLI 命令**与 `"r-shell"` MCP 键)+ 新增「桌面版下载」区,发 GitHub Release 挂三件产物(mac zip / win zip / win 安装器)。验收:`cargo test -p r-shell-core` **54 通过** / `flutter build macos` ✓ Built(80.4MB)/ Windows `flutter build windows` ✓ + `Conch.exe` + 运行时 + 安装器齐出。**待办**:CLI(`r-shell` 二进制)与 CI 仍用旧名(可后续单独 rebrand);macOS/Windows 代码签名 + 公证;最早聊到的「服务端智能命令补全」。

## 目标范围(v1 = 完整对标 CLI)

连接管理 · 多标签终端(PTY)· SFTP 文件管理 · 监控仪表盘 · MCP 开关。平台:macOS / Windows / Linux。UI 以用户提供的设计稿为基准。

## 技术决策(已定)

- UI:**Flutter**(Dart);状态管理 Riverpod;终端 `xterm.dart`;图表 `fl_chart`。
- 桥接:**flutter_rust_bridge v2** 调用 **`r-shell-core`**。
- 工具链就绪:cargo 1.96 / Flutter 3.44 / Dart 3.12 / node 26 / pnpm 11 / cmake 4.3。

## 已完成

- [x] **[2026-06-24] 内嵌 adb · 零环境交付**：新增解析器 `core/src/adb_bin.rs`（内嵌优先→env→SDK→PATH→常见目录），`adb.rs` 全部 `Command::new("adb")` 改走它；macOS `scripts/build_mac.sh`（新）把 adb 内嵌进 `Conch.app/Contents/Resources/adb` + ad-hoc 重签 + zip，Windows `build_win.ps1`（扩展）内嵌 `adb.exe`+2 dll 到 `Release\adb`，`scripts/fetch_adb.sh`（新）缓存/下载官方 platform-tools。内嵌 adb 为 universal 且仅依赖系统库，干净机双击即用。`cargo test -p r-shell-core` **57 通过**（+3 解析器测）；本机把诊断器 `core/examples/adb_which.rs` 放进 `.app` 内运行确认命中**内嵌副本**。详见 [10-Stage8-ADB §7](10-Stage8-ADB.md)。

- [x] 克隆 `r-shell-cli` 到本项目(保留 git 历史)。
- [x] 摸清架构(纯 Rust 单二进制 CLI + MCP)。
- [x] 范围定为「完整对标 CLI」。
- [x] 技术选型对比文档 `01`(评分 Tauri 57 / Flutter 49 / 纯 Rust 43)。
- [x] **决策:用 Flutter**(与项目桌面默认规则一致)。
- [x] 工具链探测全部就绪。
- [x] 基线编译通过(改造前 `cargo build` 绿)。
- [x] **Stage 0:重构为 cargo workspace,抽出 `r-shell-core` 库**,`cli` 改依赖它。
  - 模块 `ssh / ssh/tests / model / storage / native_backend / monitor / mcp` 由 `git mv` 迁入 `core/src`(保留历史)。
  - `cli/src` 仅留 `main.rs`;`Cargo.lock` 移到仓库根。
  - 结果:`cargo build` 零警告、`cargo test` **24 通过 / 0 失败 / 5 ignored**(5 个是需真实服务器的在线 SSH 测试)、`r-shell --version/--help` 冒烟正常。
- [x] 架构设计文档 `02`(分层、仓库结构、frb 接口、终端方案、UI 规格、阶段计划)。
- [x] **UI 设计稿(4 张高保真)**:主界面三栏 / SFTP 双栏 / 连接编辑弹窗 / MCP 面板,存于 `docs/gui/design/`。风格 = FinalShell 现代化(「Midnight Ops」深色 + cyan 强调,参考 Warp/Linear/Tokyo Night)。
- [x] **UI 设计规范 `03`**:设计语言、色彩/字体/间距/圆角 tokens、组件规范、终端 ANSI 色板、动效、可访问性、与 core 对齐、Dart 主题骨架。
- [x] **开工文档 `04`**:Stage 1 任务分解(T1–T7,各含验收 DoD)、目标目录结构、数据流、设计还原对照、命令清单。
- [x] **Stage 1 编码(最小闭环,macOS 跑通)**:
  - `flutter create desktop`(macos/windows/linux)+ frb `integrate`(`rust_lib_r_shell_desktop` cdylib,已入 workspace `members`、依赖 `../../core`)。
  - `list_connections()` 脱敏 API(`ConnectionDto`,无明文密码/私钥,复用 `core::storage`/`model`)+ frb 绑定生成。
  - 主题 token 落地(`shared/theme/`:AppColors/AppSpacing/AppRadius/AppTheme;`GlassPanel` 备玻璃点缀)。
  - 三栏外壳(`features/shell/app_scaffold.dart`:图标栏 + 连接树侧栏 + 中心工作区 + 监控侧栏 + 状态栏)+ 连接树(`features/connections/`:分组折叠/搜索/状态点,渲染 core 真实连接)。
  - macOS 窗口最小 1040×640;`flutter analyze` 0 告警;`flutter run` ✓ Built、Dart VM Service 正常、无布局溢出。
- [x] **Stage 2:连接增删改 + 连接编辑弹窗(macOS 跑通)**:
  - **核心(单一事实来源)**:新增 `core/src/connections.rs` —— `NewConnection` / `ConnectionPatch` + 纯函数 `build_connection` / `apply_patch` / `remove_from`(校验、trim、空分组兜底、密钥三态、删除时清理标签与 active 指针)+ `add` / `update` / `remove`(load→改→save)。8 个单测覆盖,`cargo test -p r-shell-core` 24→**32 通过**。
  - **CLI 重构**:`connections_add/update/remove` 改为委托 `core::connections`,行为不变、删除重复实现(零回归)。
  - **bridge**:`ConnectionDto` 增 `has_passphrase`;新增 `ConnectionInput` + `create_connection` / `update_connection` / `delete_connection`(密钥三态:`None` 保持 / `Some("")` 清除 / `Some(v)` 设置);frb codegen 重新生成绑定。
  - **Flutter**:`bridge/connections_repository.dart`(Repository 层);`features/connections/connection_edit_dialog.dart`(四页签:常规/SSH/高级/备注;认证方式切换;密钥字段「留空保持不变」+「清除」开关;校验失败定位到对应页签);`connection_tree.dart` 改为受控展示组件(数据由父级持有)+ 逐项 ⋯ 菜单(编辑/删除)+ 认证方式角标;`app_scaffold.dart` 接管连接状态(加载/选中/增删改后刷新)、「添加连接」入口、删除二次确认弹窗、SnackBar 反馈。
  - 验收:`flutter analyze` 0 告警、`flutter build macos` ✓ Built、`flutter run -d macos` 运行无异常、`flutter test` 通过。
- [x] **Stage 3:多标签 PTY 终端(macOS 跑通)**:
  - **核心**:`NativeConnectionManager` 增加按唯一 `pty_id` 的多 PTY 注册表(`open_pty`/`write_pty`/`read_pty`/`resize_pty_by_id`/`close_pty`,`PtyHandle` 记录所属连接;`close_connection` 连带清理其名下 PTY)。旧的按 `connection_id` 单 PTY 接口(CLI `shell` 用)原样保留——零回归。`connections` 新增 `find` 与 `build_ssh_config`(无 TTY 提示,密码缺失即报错)。
  - **bridge**:`api/terminal.rs` —— 进程级 `OnceLock<NativeConnectionManager>`;`session_open`(幂等复用)/`session_close`/`session_is_open`、`pty_open`(返回 id)/`pty_subscribe`(`StreamSink<Vec<u8>>`→`Stream<Uint8List>`)/`pty_write`/`pty_resize`/`pty_close`。凭据仅 Rust 侧使用。
  - **Flutter**:`flutter pub add xterm`(4.0.0);`features/terminal/` —— `TerminalRepository`(frb 封装,前缀导入避免命名冲突)、`TerminalSession`(单标签:xterm `Terminal` + 连接/开 PTY/订阅/输入回写/resize/重连;增量 UTF-8 解码器跨分片防乱码)、`TerminalTabsController`(多标签开/切/关 + 最后一标签关闭即断会话)、`TerminalPanel`(标签条 + `IndexedStack` 保活多个 `TerminalView` + 空状态)、`terminal_theme.dart`(Midnight Ops ANSI 配色,透明底透出渐变)。`app_scaffold` 中心区换成 `TerminalPanel`,连接树支持双击 / ⋯「打开终端」开标签。
  - 验收:`flutter analyze` 0 告警、`flutter build macos` ✓ Built、`flutter run -d macos` 启动无异常(Dart VM + Rust 静态管理器初始化无 panic)、`flutter test` 通过。**真机端到端已于 2026-06-22 验证通过**(见下条 Stage 3.5)。
- [x] **Stage 3.5:真机端到端验证 + macOS 沙箱修复(2026-06-22)**:
  - **根因(关键 bug)**:`macos/Runner/DebugProfile.entitlements` / `Release.entitlements` 开着 `com.apple.security.app-sandbox=true` 且**没有** `com.apple.security.network.client`。沙箱下无 network-client = **任何对外 socket 都被拒** → GUI 终端永远连不上;且沙箱把数据目录重定向到容器、并禁止读 `~/.ssh` 私钥与 `~/.ssh/known_hosts`。这正是此前"真机端到端没验证"的隐藏阻断。
  - **修复**:两个 entitlements **关闭 app-sandbox**、补 `network.client`+`network.server`(SSH 客户端需对外连任意主机、读任意路径密钥;本品走 DMG 分发非 MAS,与 `pgid_auto` 既有做法一致)。
  - **接入真机**:CLI `connections add` 写入共享 `workspace.json`(`~/Library/Application Support/r-shell/`,沙箱关闭后 GUI 同源读取);连接 `Win-Dev (shanh)` = `shanh@192.168.0.112:22`,publickey 绝对路径 `~/.ssh/pgid_win`(来自 `pgid_auto` 项目在该 Win 机布的 OpenSSH);先用 CLI 非 `--insecure` 跑一次把主机公钥写进 `known_hosts`(GUI 走 TOFU 即顺)。
  - **验证**:重编 `flutter run -d macos` → GUI 自动对该连接开终端 → **真实 Windows PowerShell PTY**(banner + `PS C:\Users\shanh>`),`ls D:\` 列出真实开发目录(`pgid_app.zip`/`pgid_auto_win64.zip`/`winpmem64.exe`…),交互执行 `whoami/hostname/OS Caption` 返回真实数据、中文正常。CLI 侧 `exec` 同样取到 OS/CPU(i7-9700 8C/8T)/RAM(15.9GB)/磁盘(117.5/118.1GB)/Uptime 等真实快照(注:`exec` 非交互通道走 GBK 码页,中文 OS 名有乱码;交互 PTY 走 UTF-8 正常)。截图与原始输出存于 `docs/gui/05-真机验证-win.md`。
- [x] **Stage 3.6:终端增强(命令高亮 · 历史命令 · 终端内容查询)(2026-06-22,macOS 编译验证通过)**——见 [08-Stage3.6-终端增强](08-Stage3.6-终端增强.md)。
  - **终端内容查询(解决「宝塔密码忘了」场景)**:新增 `terminal_search.dart`(`TerminalSearch`)——在终端**回滚缓冲区**做全文搜索(默认大小写不敏感,可切),命中经 `TerminalController.highlight` 用 `CellAnchor` 落色(锚点随行滚动稳定、行被挤出即自动失效,零悬挂)、当前命中青色高亮并**自动滚动进视口**(用注入的 `ScrollController` 按 `maxScrollExtent/行数` 反推行高定位);**宽字符(CJK)按 cell 列对齐**(逐 cell 重建「字符→列」映射)。工具栏 `⌘/Ctrl+F` 唤起搜索框,`Enter` 下一处、`Esc` 关闭并把焦点还给终端,`n/m` 命中计数 + 上/下/重扫。
  - **历史命令查询**:新增 `command_history.dart`——`CommandLineAccumulator` 从「用户键入流」最佳努力还原命令(处理退格 / Ctrl-C/U/W、跳过方向键等转义序列、回车收尾),`CommandHistory`(连续去重 + 限容 + 子串过滤);右侧 `command_history_panel.dart` 面板:过滤框 + 列表,点击**回填**命令到终端、▶**执行**、复制、清空。
  - **命令高亮**:新增 `command_highlighter.dart`——纯 `tokenizeCommand` 分词器(命令 / 选项 / 路径 / 字符串 / 变量 / 管道重定向 / 注释 + `sudo`/`env` 等前缀续命令位),`HighlightedCommand` 渲染语法着色;用于历史面板。**注**:交互式 PTY 输入行的实时着色归远端 shell(PSReadLine/zsh)管,本端高亮作用于历史列表等自渲染文本。
  - **接线**:`terminal_session.dart` 持有 `TerminalController`/`ScrollController`/`TerminalSearch`/`CommandHistory`,`_onUserInput` 同时喂历史,新增 `sendText({execute})`,`maxLines` 5000→10000(更可能留住装完软件打印的密码);`terminal_panel.dart` 每标签 = 工具栏 + 终端(注入 controller/scroll/focus + `onKeyEvent`)+ 右侧历史面板。
  - **验收**:`flutter analyze` 0 告警 / `flutter test` **38 通过**(原 11 + 新增 highlighter 7 + history 13 + search 7)/ `flutter build macos` ✓ Built。真机交互验证(敲命令看历史/高亮、搜一段历史输出定位+跳转)待连服务器补。

## 待办

- [x] **Stage 1(编码)**:Flutter 脚手架 + `r-shell-bridge`(frb,已入 workspace、依赖 core)+ `list_connections()` 脱敏 API + 三栏外壳 + 连接树渲染真实连接;macOS `flutter run` 跑通、`flutter analyze` 0 告警。(见 [04-开工文档](04-开工文档.md) T1–T7)
- [x] **Stage 2:连接管理(增删改 + 分组 + 连接编辑弹窗)** —— CRUD 下沉 core、CLI/GUI 同源、四页签编辑弹窗、删除确认。
- [x] **Stage 3:多标签 PTY 终端** —— core 多 PTY、bridge 流式、xterm 多标签 + 重连。
- [x] **Stage 4:SFTP 双栏 + 拖拽上传 + 传输队列** —— core SFTP `read_dir`/`realpath`/带进度收发,bridge `sftp_*` + 进度流,Flutter 双栏 + `desktop_drop` 拖拽 + 队列。(见 [06-Stage4-SFTP](06-Stage4-SFTP.md))
- [x] **Stage 5:监控仪表盘(`stats_stream` + `fl_chart`)** —— core 零改动复用 `monitor.rs`,bridge `stats_stream` 周期 `StreamSink` 推送 `SystemStatsDto`,Flutter `features/monitor/`(Repository + Controller〔环形历史/状态机/自动重试〕+ MonitorPanel〔fl_chart 走势图 + 阈值着色〕)跟随选中连接。(见 [07-Stage5-监控](07-Stage5-监控.md))
- [x] **Stage 6:MCP 面板 + 设置页(UI 优先,macOS 真机冒烟通过)** —— bridge `api/mcp.rs`(start/stop/status/tools,不改 core)+ Flutter `features/mcp/`(状态卡 ON/OFF 开关 + 端点复制 + 12 工具清单)+ `features/settings/`(外观/终端/监控/MCP/关于)。(见 [09-Stage6-MCP](09-Stage6-MCP.md))
- [ ] Stage 7:主题切换实际生效 + 设置持久化 + 打包(DMG/NSIS/AppImage)+ GUI CI。
- [x] **Stage 9:命令块终端原型(方案 A · Warp / 聊天流)** —— 每条命令+输出成块、从上往下读、输入框钉底;本机 + 远程(SSH/ADB)双目标,cwd 跨块持久 + 退出码 + stderr。core 几乎不动(复用 `execute_command` + 新增 `blockexec` 纯逻辑),Flutter `features/blockterm/` + 新导航入口。(见 [11-命令块终端原型](11-命令块终端原型.md))

## 技术债 / 后续修正

- [ ] `scripts/bump-version.*` 目前面向单 crate 的 `cli/Cargo.toml` + `cli/Cargo.lock`;workspace 化后 `Cargo.lock` 在根、且新增 `core`(及将来 `desktop/rust`)。发版前需让 bump 脚本同时更新各 crate 版本与根 lock。
- [ ] `package.json` 的 `--manifest-path cli/Cargo.toml` 脚本仍可用(只构建 CLI);可酌情加 workspace 级脚本。
- [ ] README 的「Project Structure」需在 GUI 落地后更新为 workspace 结构。
- [ ] **状态管理暂未引入 Riverpod**:Stage 1/2 沿用「状态上提到 `AppScaffold` + Repository 直调」的轻量模式(与 Stage 1 现状一致,改动小、风险低)。架构文档(02 §7)规划用 Riverpod;待 Stage 3+(终端/SFTP/监控的长生命周期资源)再统一引入,届时把连接状态一并迁过去。
- [ ] **编辑弹窗的密钥字段不回填**:遵循脱敏策略,`ConnectionDto` 不含明文密码/passphrase/私钥路径,故编辑时这些字段以「已配置 · 留空保持不变」呈现并提供「清除」开关。私钥*路径*本身非凭据,后续可考虑在 DTO 暴露以改善公钥连接的编辑体验。
- [ ] **终端 / 会话相关(Stage 3)**:
  - **[2026-06-22 UI 优化] 命令文本偏移修复**:此前 `TerminalSession` 在构造时就用「猜的」80×24 开 PTY(早于 `TerminalView` 首帧布局),随后 resize → PowerShell/PSReadLine 在 80 列绘制完又被改宽 → 首个提示符/命令重绘**错位**。改为**推迟到首帧 `onResize` 拿到真实尺寸再开 PTY**(`_sessionReady`+`_hasSize`+`_opening` 三态护栏,250ms 兜底),消除开场竞态。**注**:会话中途手动拉伸窗口仍可能有 PSReadLine 自身的一次性重排(非本端可控)。
  - **[2026-06-22 UI 优化] 读不到服务器的反馈**:终端 `error/closed` 时在底部浮一张状态卡(`_SessionStatusBanner`:`cloud_off` 图标 + 「服务器连接失败 / 会话已结束」+ 真实错误详情 + 显眼「重连」实色按钮),不再只靠标签上的小重连图标 + 缓冲区里的红字。
  - 密码认证若未存密码则连接报错(GUI 无 TTY 提示);后续应加「连接时弹密码框」交互。
  - PTY 输出用 150ms 超时轮询 `read_pty` 的方式推流(空闲每 150ms 唤醒一次感知关闭);可改为纯事件驱动以更省。
  - 会话仅在「某连接最后一个终端标签关闭」时断开;尚无显式「断开连接」按钮(留待设置/状态栏)。
  - 终端工具栏(复制 / 清屏 / 搜索)、`TerminalController` 选择复制、按连接复用 PTY 的 SFTP 同会话等,后续阶段补。
  - `xterm` 引入了 `zmodem` 等传递依赖;`flutter pub outdated` 提示 matcher/meta 等有新版本(受约束未升),非阻塞。
- [ ] **SFTP / 文件管理相关(Stage 4)**:
  - ✅ **[2026-06-23] 删除 / 重命名 / 打开编辑 / 复制到剪贴板 / 右键菜单 / 跨栏拖拽收发已落地**(SSH/ADB 双栏通用,见关键决策 2026-06-23 末条)。仍待补:目录(递归)传输、多选批量、新建目录、断点续传、取消进行中任务。
  - 软链接(symlink)在远程栏可点进(走服务端 realpath),但当作文件下载会失败;尚未区分「链接指向目录」。
  - 进度在 bridge 端按 128KB 节流;无传输速率 / 剩余时间估算(可在 Dart 端用进度差分算)。
  - 真机端到端 SFTP 收发**待连真实服务器验证**(已编译验证;逻辑与 Stage 3 真机会话同源,复用已开会话)。
  - 新增 `desktop_drop` 依赖(桌面拖拽);macOS 已关沙箱故拖拽读本地路径与本地栏 `dart:io` 浏览均可用。
  - **⚠️ frb 流错误通道隐患(Stage 5 实测确认,Stage 4 同款未验)**:`sftp_upload/download` 返回 `Result<(),String>`,失败时 frb 把 `Err` 放进 unawaited Future,**Dart `Stream.onError` 收不到** → 传输失败会变成控制台 Unhandled Exception,且 `transfer_queue` 走的是 `onDone`(误判为「完成」)。修法同 Stage 5:让 `sftp_*` 永不外抛,把成功/失败都封装进数据帧(如给 `TransferProgress` 加 `error: Option<String>`)走 sink。连真实服务器做失败用例时一并修。
- [ ] **监控相关(Stage 5 / 功能阶段)**:
  - ✅ **[2026-06-22 功能阶段] Windows 远程监控已落地**:`core/monitor.rs` 新增 `windows_stats_command()`(PowerShell CIM 一次往返:CPU `LoadPercentage`/内存/页面文件/系统盘/`Win32_PerfRawData_Tcpip_NetworkInterface` 累计字节/uptime/OS,首行 `RSHELL_WIN` 哨兵,输出强制 UTF-8 修中文 OS 名)+ `parse_windows_snapshot()` + `StatsSnapshot.cpu_percent_direct`(Windows 无 jiffies,直接给 CPU%,首帧即有)+ `StatsPlatform`;`native_backend` 新增 `fetch_system_snapshot()`(**先试 Linux,失败/空则回退 Windows,按连接缓存平台**,稳态单次往返)+ `close_connection` 清缓存;bridge `collect()` 与 CLI `cmd_stats` 改用之。**真机已验证**:CLI `stats` 与 GUI 监控侧栏均对 `shanh@192.168.0.112`(Win11)取到真实 CPU/内存/磁盘/网速/uptime + 正确中文 OS 名(截图 `docs/gui/screenshots/win_monitor_live.png`)。纯命令耗时 ~1.2s(CIM,WMI 冷启动首次约 8s)。
  - **Linux 真机 live 数据/图表验证待补**:成功取数逻辑与 CLI `stats` 同源(Windows 已真机验证;Linux 路径未变,复用已开会话)。
  - macOS 远端监控仍为 Linux 命令(连 macOS 主机会回退失败 → 错误态);如需可再加 `top`/`vm_stat` 采集器,后续补。
  - 采样间隔固定 2s(代码可调,UI 未暴露,留 Stage 7 设置页);历史不持久(切连/重启清空,监控为瞬时量无需持久)。
  - 选中连接即触发 `ensure_session`(可能为只想浏览的连接建立 SSH 会话);如需「仅终端/文件时才连」可后续加开关。
  - 新增 `fl_chart` 依赖(1.2.0)。
- [ ] **pre-existing clippy 提示(非本阶段引入,非阻塞)**:`core` 有 9 条风格类 clippy 警告(`mcp.rs` collapsible_if ×5、`model.rs` derivable_impls、`monitor.rs` field_reassign_with_default + collapsible_if、`ssh.rs` `SshClient` 缺 `Default`);`cargo build --workspace` 本身零警告。属既有技术债,留待统一清理(本阶段只顺手修了 CLI 的 1 条 `print_literal`)。

## 关键决策记录

- 2026-06-20:GUI v1 范围 = 完整对标 CLI。
- 2026-06-20:选型先出对比再决策;**最终选 Flutter**(评分最高是 Tauri,但取项目桌面默认 + UI 一致性)。
- 2026-06-20:Stage 0 完成——`r-shell-core` 抽离,CLI/GUI/MCP 同源;零回归。
- 2026-06-22:确定 UI 设计方向 = **FinalShell 现代化**(深色「Midnight Ops」+ cyan 强调);产出 4 张高保真设计稿 + 设计规范(`03`)+ 开工文档(`04`);Stage 1 进入编码准备。
- 2026-06-22:**Stage 1 落地完成**——Flutter + flutter_rust_bridge 打通,连接树渲染 `r-shell-core` 真实连接,macOS 编译运行通过。UI 决策:**实色 Midnight Ops 主体 + 液态玻璃点缀**(融合项目毛玻璃默认规则与已认可的实色设计稿;运维信息密集区保持高对比实色,玻璃用于 chrome/overlay)。`desktop/rust` 已正式加入根 workspace。
- 2026-06-22:**Stage 2 落地完成**——连接增删改 + 四页签编辑弹窗。关键决策:**CRUD 逻辑下沉到 `core::connections` 作为单一事实来源**,CLI 重构为委托调用(零回归),GUI bridge 同源调用,避免逻辑漂移;密钥字段采用 `None/Some("")/Some(v)` 三态语义,编辑时「留空保持不变 + 清除开关」;状态管理本阶段仍走「上提到 AppScaffold」的轻量模式(Riverpod 推迟到 Stage 3+)。
- 2026-06-22:**Stage 3 落地完成**——多标签 PTY 终端。关键决策:**core 增加「按 pty_id 的多 PTY」能力**(支持同主机多标签),旧 connection_id 单 PTY 接口保留给 CLI(零回归);bridge 用 `StreamSink<Vec<u8>>` 单向推流,Dart 端做**增量 UTF-8 解码**(跨分片暂存不完整多字节序列)防乱码;选用 `xterm`(TerminalStudio)4.0.0;会话**幂等复用 + 最后标签关闭即断**;凭据只在 Rust 侧用。状态仍走 ChangeNotifier(`TerminalTabsController`/`TerminalSession`)轻量模式,Riverpod 继续推迟。真机端到端 SSH 验证待有可达服务器时补。
- 2026-06-22:**Stage 3.5——真机端到端跑通 + 修复 macOS 沙箱致命阻断**。关键发现:GUI 之所以从没真机验证成,是因为 `*.entitlements` 开 `app-sandbox` 又缺 `network.client`,沙箱直接掐断所有对外 SSH、并挡住读 `~/.ssh` 私钥。决策:**关闭 app-sandbox + 补 network client/server**(SSH 客户端的合理诉求,DMG 分发非 MAS;与 `pgid_auto` 一致)。用局域网真实 Win 开发机(`shanh@192.168.0.112`,publickey)在 GUI 终端跑出真实 PowerShell 会话与数据,Stage 3 真机验证 ✅ 收口。
- 2026-06-22:**Stage 5 落地完成**——监控仪表盘实时接入 + 走势图。关键决策:① **core 零改动**——监控逻辑(`stats_command`/`parse_snapshot`/`SystemStats::from_samples` + `native_backend::fetch_system_stats`)早已完整且有单测(CLI `stats` 在用),GUI 直接复用,差分(CPU%/网速需两帧)与采样调度放 bridge;② **bridge `stats_stream` 用 `StreamSink<SystemStatsDto>` 周期单向推流**(与终端 PTY / SFTP 进度同范式),`ensure_session` 复用共享会话、**不主动关闭**(终端/SFTP 可能在用,由终端「最后标签关闭」统一回收),首帧 `previous=None` 故 CPU%/网速为 0、内存/磁盘/负载/uptime/OS 立即可用;③ **采样间隔默认 2s**(设计稿默认 1s「可配」,但远端每帧一条 SSH 命令,2s 更省;Rust 兜底下限 250ms);④ **格式化放 Dart 侧**(同 SFTP;`monitor_format.dart` 对照 Rust 版 + 单测固定防漂移),Rust 只回传干净数值(f64/i64);⑤ **走势图用 `fl_chart`**(1.2.0)极简 `LineChart`(无网格/轴/触摸 + 顶部渐变填充,网络双线共享 Y 轴),阈值 >80% warning、>92% danger;⑥ **MonitorController 跟随选中连接**(`_syncMonitor` 在各选中入口调用)、环形历史(60 点)、`idle/connecting/live/error` 状态机,状态延续轻量 `ChangeNotifier`(Riverpod 继续推迟);⑦ **错误走数据帧而非 frb `Err`(重要经验)**——frb 对 `async fn(sink)->Result<(),E>` 把函数 `Err` 放进 unawaited Future,Dart `onError` 收不到→变 "Unhandled Exception";故 `stats_stream` 永不外抛、失败封装进 `StatsFrame.error` 走 sink + Rust 内部自重试(主机恢复自动回到 sample),Dart `onData` 分支处理(同款隐患在 Stage 4 `sftp_*`,记技术债)。验收:`cargo build` 零警告、`cargo test` 32 通过、`flutter analyze` 0 告警、`flutter test` 11 通过、`flutter build macos` ✓ Built、`flutter run` 真机冒烟(Windows 机优雅降级 + 0 未捕获异常)。**限制**:指标命令 Linux 专用,连非 Linux 主机命令非零退出 → 显示『监控已断开』错误态(优雅降级);Linux 真机 live 验证待补。
- 2026-06-22:**Stage 4 落地完成**——SFTP 双栏文件管理 + 拖拽上传 + 传输队列。关键决策:① **列目录改用 SFTP `read_dir`**(结构化元数据,跨平台含 Windows OpenSSH;旧 `ls -la` 接口原样保留给 CLI,零回归);② **进度用回调闭包 `FnMut(u64,u64)` + bridge 端 128KB 节流**(core 不依赖 frb,大文件不刷爆 stream),收发经 `StreamSink<TransferProgress>` 走 frb 流(进度 onData / 失败 onError / 完成 onDone);③ **进程级 `manager()` 上提共享 + `ensure_session`**——SFTP 与终端**复用同一活跃 SSH 会话**(关键:文件操作不另开连接);④ **数值(size/mtime/进度)用 i64 干净映射 Dart `int`,体积 / 时间格式化交给 Dart**;⑤ **本地一侧用 `dart:io` 浏览**(与连接解耦),拖拽用 `desktop_drop`;⑥ 状态延续轻量 `ChangeNotifier`(`FilesController`/`TransferQueue`),Riverpod 继续推迟。验收:`cargo build` 零警告、`cargo test` 32 通过、`flutter analyze` 0 告警、`flutter test` 6 通过、`flutter build macos` ✓ Built;真机 SFTP 收发待连真实服务器补。
- 2026-06-22:**功能阶段(一):Windows 远程监控**——用户「现在改功能,以我的 win 做服务器测试」。关键决策:① **不在 GUI 端检测,落到 core**——`fetch_system_snapshot` 统一返回 `StatsSnapshot`,「先试 Linux(`/proc`+`df`),`Err`/`is_empty()` 则回退 Windows(PowerShell CIM)」,结果**按连接缓存 `StatsPlatform`**(稳态单次往返;断开清缓存);bridge `collect` 与 CLI `stats` 改用之(原 `fetch_system_stats`+`parse_snapshot` 退役但保留)。② **Windows 命令只用单引号 + 强制 `[Console]::OutputEncoding=UTF8`**——规避 Windows-OpenSSH exec 的引号 mangling,并修掉中文 OS 名经 GBK 码页变乱码(真机实测:不加→乱码,加→正确)。③ **CPU 无 jiffies 走 `cpu_percent_direct`**——Windows `LoadPercentage` 是瞬时百分比,`from_samples` 在缺 jiffies 时用它,故 Windows 首帧即有 CPU%;网络用 `Win32_PerfRawData_*` 的**累计**字节,沿用两帧差分出速率。④ **先在真机把 PowerShell 命令调通再写进 core**(用与 russh exec 等价的「单参数不二次展开」方式发送),纯命令耗时 ~1.2s(CIM)。验收:`cargo build` 零警告、`cargo test` **35 通过**(+3 新)、`flutter analyze` 0 告警;**真机端到端**:CLI `stats` 与 GUI 监控侧栏对 Win11(`shanh@192.168.0.112`)取到真实 CPU/内存/磁盘(99.7% 红)/网速/uptime + 正确中文 OS 名。**待续**:设置项真生效+持久化、SFTP 收发 e2e + `sftp_*` 流错误通道、终端密码框。
- 2026-06-22:**Stage 6 落地完成**——MCP 面板 + 设置页(UI 优先,按用户「先把 UI 页面全部做完,再修功能」)。关键决策:① **MCP 起停不改 core**——core `start_mcp_server` 原是 CLI 阻塞运行;GUI 侧在 bridge `api/mcp.rs` 用 `tokio::spawn` 跑它、存 `JoinHandle`,停止即 `abort()`(drop axum serve future → 释放监听端口);运行态/运行时长由 **bridge 私有静态 `Mutex<McpState>`** 自管,**不复用 core 的 `MCP_SERVER_RUNNING`**(abort 不会回写它);start 后 sleep 250ms 探测任务是否秒退来判定端口占用并报错(`mcp_start` 因此为 async)。② **工具目录用静态镜像而非运行时反射**——rmcp 的 `#[tool]` 无现成枚举接口,`mcp_tools()`(sync)硬编码 12 条(name + 中文说明 + 分类),与 core `RShellMcpServer` 一一对应,低风险、易维护。③ **状态读取走 sync frb**(`mcp_status`/`mcp_tools`/`mcp_endpoint`/`mcp_port`),控制器构造期直接同步取数(与连接列表同范式);`uptime_secs` 是 `u64`→Dart `BigInt`,`.toInt()` 取用。④ **监控侧栏按视图显隐**——`app_scaffold._showMonitor` 仅在 连接/终端/监控 为真,文件/MCP/设置让中心页占满(对照设计稿 SFTP/MCP 无监控栏)。⑤ **设置页本阶段内存态**——`SettingsController` 先承接所有项的状态,持久化(`shared_preferences`)+ 实际生效(采样间隔接 MonitorController、字号接终端、主题切换、MCP 自启)留到 Stage 7「修功能」阶段,符合用户「UI 先行」诉求。⑥ 状态延续轻量 `ChangeNotifier`(Riverpod 继续推迟,与前序阶段一致)。验收:`cargo build` 零警告、`cargo test` 32 通过、`flutter analyze` 0 告警、`flutter test` 38 通过、`flutter build macos` ✓ Built;**真机冒烟**:GUI 点「启动」→ 端口 9123 实测 LISTEN(本 app 进程)+ UI 转「运行中」逐秒计时;设置页控件可交互。
- 2026-06-22:**Stage 3.6 落地完成**——终端增强三件套。关键决策:① **终端内容查询走「缓冲区 + 稳定锚点高亮」而非自建文本视图**——`xterm` 4.0.0 无内置搜索,但提供 `buffer.getText/lines`、`createAnchor`(随行滚动稳定、行被挤出自动失效)、`TerminalController.highlight`,据此自实现搜索 + 落色 + 滚动定位(行高由 `maxScrollExtent/(总行-视高)` 反推);**宽字符按 cell 列对齐**(逐 cell 重建「字符→列」映射,规避 `getText` 把宽字折叠成 1 字符导致的错位)。② **历史命令从「键入流」最佳努力还原**(非解析回显):累积可见字符、处理行编辑控制字符、跳过转义序列;**已知局限**:↑ 召回 / Tab 补全来自远端回显不计入(记技术债)。③ **命令高亮限定在自渲染文本**(历史面板),不抢远端 shell 的实时输入着色。④ **纯逻辑(分词 / 行匹配 / 命令抽取)全部抽成可单测函数**(+27 用例),UI/会话用轻量 `ChangeNotifier`(Riverpod 继续推迟,与前序阶段一致)。⑤ `maxLines` 提到 10000 以更好留住「装完软件打印的密码」。验收:`flutter analyze` 0 告警、`flutter test` 38 通过、`flutter build macos` ✓ Built。
- 2026-06-22:**Stage 8 落地(编码完成,真机待验)**——ADB 通道(用 R-Shell 管安卓,用户「方案 B:rust 实现 + flutter UI」)。关键决策:① **后端用 `Backend` enum 而非 `dyn Trait`**(SFTP 收发是泛型回调 `FnMut(u64,u64)`,trait object 容不下泛型方法),manager 注册表统一 `Arc<RwLock<Backend>>`,exec/PTY/SFTP/监控一次 `match` 分派转发,**bridge 与 Flutter 对后端透明**(复用既有 `pty_*`/`sftp_*`/`stats_stream`);② **`PtySession` 抽到 `core/pty.rs` 去掉 russh `channel_id`**(原字段无人读),SSH(russh channel)与 ADB(`adb shell -t -t` 子进程)共用同一结构;③ **ADB 是「子进程后端」**——本机 `adb` daemon 保活会话,`AdbClient` 几乎无状态,每操作 spawn `adb`(exec=`shell <cmd>` 单参数交设备 `sh -c`;文件=push/pull/`exec-out cat`/临时文件+push;列目录=`ls -la`→`parse_android_ls`);④ **监控零成本复用 Linux 路径**(安卓 toybox 有 `/proc`+`df`+`nproc`+`uname`),GUI **每秒刷新**;⑤ **连接后 best-effort harden**(`settings put global development_settings_enabled/adb_enabled/stay_on_while_plugged_in`,adb shell uid=2000 默认持 `WRITE_SECURE_SETTINGS`,**无需 root**)对抗用户反馈的「开发者模式容易掉」;⑥ `model.normalized` 放行 ADB(原**强制只留 SSH 并锁 protocol**),`connections` 加 `protocol`(New/Patch),ADB 放宽 username/auth 校验,**CLI 补 `protocol:\"SSH\"` 零回归**;⑦ bridge `ensure_session` 按 `connection.protocol` 分派。**限制**:终端动态 resize / 传输百分比进度 / `ls` 的 mtime 暂略;持久授权(写 `/data/misc/adb/adb_keys`)与 `persist.adb.tcp.port` 需 root,待真机 `adb root` 固化。验收:`cargo build` 零警告、`cargo test` 37 通过、`flutter analyze` 0 告警。**2026-06-23 真机验证通过 + 收口**——无线调试配对码授权后,`core/examples/adb_smoke.rs`(GUI bridge 同一套 API)对 Redmi `220233L2C`(Android 11)端到端跑通 命令/列目录/读写/上传下载/监控/交互 PTY 全绿;真机暴露并修复 **`/sdcard` 软链接目录列空**(`list_dir` 补尾斜杠);`adb root` 被生产版拒(无 root 持久化方案见 Stage8 文档 §6);workspace 安卓条目修正为 `protocol=ADB`。详见 [10-Stage8-ADB](10-Stage8-ADB.md)。
- 2026-06-23:**功能修复——监控「多盘」+ 安卓磁盘读数纠错**(用户反馈安卓磁盘数值不对、要多盘)。**两个根因**:① 旧版 `df -kP /` 只取 `/`(安卓 = 2.7G **只读系统分区** dm-3,100%),根本不是用户关心的内置存储 → 改为 `df -kP` 全量,主盘取**最大真实卷**(安卓即 `/data` = 52G/37G,对得上 64G 手机);② 安卓 toybox `df -kP` 实际按 **512 字节块**输出(表头写的是 `512-blocks`,与 Linux coreutils 的 1024 冲突)→ 不读表头会**所有容量翻倍**。**实现**:`monitor.rs` 新增 `parse_disks`(**读表头判块大小** 512/0.5·1024/1.0、**跳过 `echo MARKER` 造成的前导空行**找真表头、按来源名 + 挂载前缀**过滤**伪/系统文件系统〔tmpfs/cgroup/apex squashfs/`/dev`·`/sys`·`/apex`·`/mnt`… 〕、对**同尺寸的 bind/fuse 镜像去重**〔`/storage/emulated`=`/dev/fuse` 是 `/data` 的镜像,只留一条〕、降序 + 截断 `MAX_DISKS=8`)+ `primary_disk`(最大卷),删掉旧 `parse_df`;`SystemStats`/`from_samples` 带上 `disks: Vec<DiskUsage>`;`windows_stats_command` 改 `Get-CimInstance Win32_LogicalDisk|DriveType=3` 枚举全部本地盘发 `DISK=盘符|总KB|空闲KB` 行、`parse_windows_snapshot` 解析进 `disks`(主盘仍为系统盘)。**bridge**:`SystemStatsDto` 加 `DiskUsageDto{mount,used_kb,total_kb,percent}` 列表 + frb 重生成。**Flutter**:`monitor_panel` 把单一磁盘进度条换成**多盘卡**(主盘 hero 大数值 + 挂载点/used/total,分隔线下逐卷列「挂载点 + 百分比 + 细进度条 + used/total」,标题右「N 个卷」)。**真机验证**(`adb_smoke` 走 GUI 同套 API,设备 `192.168.0.101:5555`):主盘 `/data 70.8% (36.7/51.8 GB)`〔与 `df -h` 的 37G/52G/71% 一致,修复前翻倍成 103.6 GB〕,另列 `/`(2.7G)·`/cust`·`/vendor`·`/product`,`/storage/emulated` 已去重。验收:`cargo build --workspace` 零警告 / `cargo test` **39 通过**(+2:安卓 512 块解析、Windows 多盘)/ `flutter analyze` 0 告警 / `flutter build macos --debug` ✓ Built 并已重启 GUI。**关键教训**:`df` 容量单位**必须读表头**(POSIX `-P`=512 与 `-k`=1024 冲突,安卓 toybox 取 512);分节命令的 section 文本**带前导空行**,解析表头要先跳空行(单测要复现这一格式,否则漏判)。
- 2026-06-23:**连接认证增强 + ADB 配对码 + 本机监控**(用户三连需求:「连接时加选择密钥 / 输入密钥;安卓首连的配对码;加本机监控」)。**① 私钥选择器 + 口令(纯 Flutter)**:连接弹窗 SSH「公钥」认证下,私钥字段右侧加**原生文件选择器**(`flutter pub add file_picker` 11.0.2,API 是静态 `FilePicker.pickFiles()` 而非旧版 `.platform`;**本应用 macOS 关了沙箱故先调 `FilePicker.skipEntitlementsChecks()`** 否则 entitlements 检查异常),并把**私钥口令(passphrase)从「高级」页签提到 SSH 页签**与私钥放一起;`_secretField` 加可选 `trailing` 放「选择」按钮。后端无改动(`privateKeyPath`/`passphrase` 早已走 `ConnectionInput`)。**② ADB 配对码首连(Rust+bridge+Flutter)**:core `adb.rs` 加自由函数 `pair(host_port, code)`(`adb pair`,校验 "successfully paired")与 `connect_device`(`adb connect`);bridge 新增 `api/adb.rs`(`adb_pair`/`adb_connect`,直接调本机 adb,**无需先存连接**)+ mod 注册 + frb 重生成;Flutter 新增 `adb_pair_dialog.dart`(输入「IP:配对端口」+ 6 位配对码 → 调 `adbPair` → 成功回传设备 IP),连接弹窗 ADB 区加「用配对码配对(首次连接)」按钮,成功后预填 host=IP、port=5555。**③ 本机监控(Rust+bridge+Flutter)**:`cargo add sysinfo`(0.39.5,**`default-features=false` 只留 `system/disk/network`**——默认的 `user` 模块在 macOS 会拉 OpenDirectory 框架,Flutter 静态链接缺 `_ODNodeCreateWithName` 等符号致 BUILD FAILED;`cargo build` 不暴露此问题,**只在 Flutter app 最终链接才炸**);core 新增 `local.rs`(`LocalCollector`:跨采样复用 `sysinfo` 句柄〔CPU% 需两次刷新增量〕,产出与远端**同一种** `StatsSnapshot`〔字节→KB、net 累计字节留给 `from_samples` 差分〕,**复用既有 DTO/UI/差分**);bridge `local_stats_stream(interval_ms, sink)`(同 `stats_stream` 范式但数据来自本机、几乎不失败故恒推 sample);Flutter `StatsRepository.localStream` + **`MonitorController` 重构为「代次令牌 + 流工厂」**支持 `setLocal()`(替代原先靠 connectionId 比对过期帧的方式,统一覆盖连接 / 本机)+ `isLocal`;`MonitorPage` 支持本机模式(`connection==null && isLocal` 仍渲染、头部标「本机·这台电脑」);连接主页顶部加**「本机监控」hero 卡**(→ `_openLocalMonitor` 切监控页 + `setLocal`)。**验收**:`cargo build` 零警告 / `cargo test` 39 / `flutter analyze` 0 / `flutter test` 38 / `flutter run -d macos` **真机三项全部跑通**——本机监控显示真实数据(CPU 21% 10 核负载 3.78、内存 59% 14.2/24GB、磁盘 73% 2 卷、网络 ↓7.4/↑10.8 KB/s、macOS 26.5.1 Tahoe、uptime 1d4h);公钥认证显示「私钥文件 + 选择」按钮;ADB 区显示「用配对码配对」按钮。截图 `docs/gui/screenshots/{connections_with_local,local_monitor,dialog_key_picker,dialog_adb_pair}.png`。**关键教训**:① sysinfo 在 Flutter/macOS 静态链接务必 `default-features=false` 砍掉 `user`(OpenDirectory)否则链接缺符号,且该错 `cargo build` 不报、只在最终 app 链接报;② file_picker 11 API 改为静态方法 + 关沙箱要 `skipEntitlementsChecks()`。
- 2026-06-23:**导航页面拆分修复**(用户反馈「连接的界面没做出来,连接和监控是同一个页面,很不符合」)。**根因**:`app_scaffold._center()` 的 switch 里「连接」(nav 0) 与「监控」(nav 3) 都 fallthrough 到默认分支 `_ => TerminalPanel`,且 `_showMonitor` 对连接/终端/监控都为真 → 点「连接」「终端」「监控」中间都是终端面板 + 右侧监控侧栏,三页雷同,且根本没有专门的连接主页。**修复**:① 新建 **`features/connections/connections_dashboard.dart`**(连接主页:顶栏「N 个连接 · M 在线」+ 添加连接;按分组铺**连接卡片网格**〔`LayoutBuilder` 按 ~320px 自适应列数〕,每卡 = 状态点 + 名称 + 协议角标〔SSH cyan / ADB 安卓绿 + android 图标〕+ `user@host:port`/`adb·host:port` + 认证方式 + 在线态 + 操作行〔终端 / 文件 / 监控 / 编辑 / 删除〕,单击选中、双击开终端);② 新建 **`features/monitor/monitor_page.dart`**(监控整页仪表盘:复用 `MonitorController`,宽屏 `Wrap` 自适应铺 CPU / 内存 / 磁盘主盘三张 hero 卡 + 网络双线大图 + 系统信息 + 多盘明细,空态引导「前往连接」);③ 抽出 **`features/monitor/monitor_widgets.dart`**(`MetricSparkline` + `MetricSeries` + `monitorThresholdColor`),`monitor_panel.dart`(侧栏)与 `monitor_page.dart`(整页)共用,删掉 panel 里的私有 `_Sparkline/_Series/_thresholdColor`;④ `app_scaffold` 接线:`_center()` 给 `_navConnections`→`ConnectionsDashboard`、`_navMonitor`→`MonitorPage`;新增 `_openMonitor(c)`;**`_showMonitor` 收窄为仅 `_navTerminal`**(对照主界面设计稿:终端 + 右侧监控栏),连接/监控各自整页占满。验收:`flutter analyze` 0 问题 / `flutter test` 38 通过 / `flutter run -d macos` 真机渲染连接主页正常(分组 + 卡片 + 角标)、运行日志 0 异常。截图 `docs/gui/screenshots/connections_dashboard.png`。
- 2026-06-23:**修复「终端首屏提示符被顶到屏幕中部」**(用户:「将命令行往上移到合适的位置」,连 Win-Dev PowerShell 时提示符浮在终端中间、上方一大片空白)。**定位手段**:写 `core/examples/pty_capture.rs`(走 `NativeConnectionManager` 同 GUI bridge 一套)抓真机 PTY 启动原始字节 + Dart 探针把字节喂回真 `xterm.Terminal` 看缓冲落点(均为一次性诊断,已删)。**根因(竞态,非远端也非 xterm 渲染)**:① 远端 PowerShell 启动字节本身**干净**(查询序列 + 横幅 + 提示符,顺序输出,提示符在第 5 行);单喂这串字节、任何「先小后大」的 resize **回放都落顶部**(xterm grow 是在底部补空行、内容留顶)。② **真正的触发是「远端 shell 正在打印横幅时收到 window-change」**:此刻 ConPTY 会发一次 **`ESC[6n`(DSR 光标查询)+ `ESC[N;1H`(绝对定位)** 的重排,`N` = 终端在被查询那一刻回报的光标行;真机抓取证实——对 `ESC[6n` 回 `1` → ConPTY 发 `ESC[1;1H`、提示符落第 5 行(顶部);回 `6` → 发 `ESC[6;1H`、横幅+提示符整体下移、提示符落第 10 行、上方 5 行空白。GUI 里横幅已在小尺寸下画了若干行(光标行更大),提示符就被顶到中部。③ **谁制造了这次启动期 resize**:开终端标签时布局抖动(监控侧栏出现 → 中心区尺寸变化连发多次 `onResize`)+ 旧的 250ms 兜底用 80×24 开 PTY 后又被改成真实尺寸——都在 shell 启动窗口内下发了 window-change。**修复(`terminal_session.dart`,纯 Dart、不改 core/bridge)**:① **开 PTY 前防抖** `_scheduleOpen`(`_openDebounce=180ms`):每次 `onResize` 重置定时器,等布局尺寸稳定后**一次性按最终尺寸开 PTY**,从根上消除「开后立刻 resize」;② **打开后启动宽限期** `_startupGrace=800ms`:期内到达的 resize 先合并、到期一次性下发(此时提示符已画完、空闲态 resize 不触发上述重排——真机抓取已验证空闲 resize 远端零输出);③ 兜底 250ms→600ms(避免抢在防抖前用猜的尺寸开);`dispose` 取消定时器。验收:`flutter analyze` 0 问题 / `flutter test` 38 通过 / `cargo build -p r-shell-core` 零警告 / `flutter build macos --debug` ✓ Built。**关键教训**:① Windows OpenSSH/ConPTY 的提示符定位由「shell 启动期间是否被 resize」决定,**绝不能在远端 shell 画首屏时下发 window-change**;② 这类「只有真机实时交互才暴露」的 bug,离线静态回放(字节→xterm)会得出「一切正常」的错误结论,必须连真机抓「带 DSR 应答的实时字节」才能复现根因。
- 2026-06-23:**文件管理器交互增强落地**——右键菜单 + 打开编辑 + 重命名 / 删除 + 跨栏拖拽收发。关键决策:① **操作能力一律下沉 core 的 `Backend` enum 分派**,SSH(russh SFTP)与 ADB(`rm -rf`/`mv`/`exec-out cat`/临时文件 push)对 bridge / UI **完全透明**,新增的删除 / 重命名 / 读写文件**两种后端零分支**复用;② **bridge `sftp_*` 仍走 `async fn(...)->Result`**(删除 / 重命名 / 读写是一次性请求-响应,非长流,可用 `Result`;长流的进度上传 / 下载才需「错误走数据帧」那套,见 Stage 5 教训);③ **「打开编辑」用内置文本编辑器而非外部程序**——先 `read_file` 取字节,**含 NUL 即判二进制拒编、非 UTF-8 拒编**(避免把二进制改坏),保存时 `utf8.encode` 回写;④ **本地一侧操作用 `dart:io`**(`File/Directory` delete/rename/读写),与远程对称封装,UI 用同一套菜单 / 对话框;⑤ **「复制到剪贴板」复制文件本体**(macOS `osascript set the clipboard to POSIX file`、Windows `Set-Clipboard -LiteralPath`,Linux 无统一接口→返回 false 优雅退化),远程文件先下到 `tempDir` 再放剪贴板;⑥ **跨栏拖拽 = 既有上传 / 下载的另一入口**(远程文件 `Draggable`→本地 `DragTarget` 即下载、反向即上传,复用传输队列,目录暂不拖拽);⑦ 动作方法统一返回 `String?`(null=成功,否则错误信息→UI 弹 SnackBar),删除走二次确认弹窗。状态延续轻量 `ChangeNotifier`(Riverpod 继续推迟)。验收:`cargo build` 零警告 / `cargo test` 39 通过 / `flutter analyze` 0 问题 / `flutter test` 38 通过。**待补**:目录递归传输 / 多选批量 / 新建目录 / 断点续传;`sftp_*` 进度流错误通道技术债。
- 2026-06-23:**终端板块恢复监控侧栏 + 卡顿排查**(用户:「之前终端旁边的服务器监控加回来,还有好卡」)。**① 监控侧栏恢复**:`_showMonitor` 从「仅传统终端」放宽回 `_navIndex == _navTerminal`——**传统终端 + 命令块两个视图都带右侧服务器监控栏**(此前我把它在命令块模式下隐藏了,与用户预期不符);`_onNav` 进终端时也 `_syncMonitor()`,监控跟随当前选中连接(同既往)。**② 卡顿排查与缓解**:把 `MonitorPanel` 包进 `RepaintBoundary`——它每秒刷新只重绘自己的图层,不再波及终端 / 命令块区域(此前已是独立 `ListenableBuilder` 子树重建,现进一步隔离绘制)。**根因诊断**(非单一 bug):**(a)** 这是 **debug 构建**(JIT + 断言,比 release 慢 5–10×,macOS 还启用了实验性「merged UI/platform thread」更易卡);**(b)** 排查时实测系统负载高——`StorageManagementService` 62% + `ApplicationsStorageExtension` 47% CPU(macOS 存储/索引服务,疑似被并发的 docker/cargo 大量构建产物触发),叠加用户同时跑的 `docker build`、`cargo run --example yidun_click_stable`(已跑 275s+)、`flyenv`、多个 cargo build,CPU/IO 争用下 debug 应用必然顿;**(c)** 监控默认 1s 刷新,远端 Windows 走 PowerShell CIM 实际 ~1.2s/次。**建议**:出 `flutter build macos --release` 版判真实流畅度(待用户确认)。验收:`flutter analyze` 0 问题 / `flutter build macos --debug` ✓ Built / 重启无异常、日志无 skipped-frames。
- 2026-06-23:**命令块设为终端板块默认 + 输入语法高亮 + 命令提示补全**(用户:「把终端和命令块合成终端的默认功能。然后输入命令时要有高亮和命令提示」)。**做法(纯 Flutter,core/bridge 零改动)**:① **默认视图改命令块**——`app_scaffold._blockMode` 初值 `false→true`(点「终端」导航默认进命令块;双击连接打开 PTY 时 `_openTerminal` 仍 `_blockMode=false` 切回传统 xterm 交互终端);② **输入语法高亮**——`block_input_bar.dart` 新增私有 `_CommandEditingController extends TextEditingController`,重写 `buildTextSpan` 复用既有 `command_highlighter.dart` 的 `highlightCommandSpans`(命令名 accent / 选项 warning / 字符串 online / 变量 teal / 操作符 indigo / 注释 muted / 路径 secondary),边敲边着色(`buildTextSpan` 随 controller 通知自动重绘,无需监听);③ **命令提示补全**——`block_models.dart` 加纯函数 `suggestCommands(history, prefix, {common, limit})`(最近历史优先去重 + `kCommonCommands` 常用命令兜底,前缀匹配、排除完全相同、限 6 条,**6 个单测**)+ controller 暴露 `suggest(prefix)`;输入框上方弹补全下拉(`_SuggestionRow`:bolt 图标 + 高亮命令 + 选中态 `surfaceActive` + `Tab` 角标),交互:**有补全时 ↑/↓ 选、Tab/点选接受、Esc 收起;空输入或无匹配时 ↑/↓ 仍是命令历史**(用 `TextField.onChanged` 只在用户键入时重算建议,程序回填不误弹);④ **顺手**把命令块提示符里的命令文本也换成 `highlightCommandSpans`(命令头与输入框配色一致)。验收:`flutter analyze` 0 问题 / `flutter test` **48 通过**(原 42 + suggestCommands 6)/ `flutter build macos --debug` ✓ Built / `flutter run -d macos` 真机启动无异常(默认进命令块视图)。**注**:交互式 PTY(xterm)的实时输入着色 / 补全仍归远端 shell(PSReadLine/zsh)管,本端高亮 + 补全作用于命令块输入框。
- 2026-06-23:**命令块并入「终端板块」**(用户:「把命令块的终端功能搬到终端板块那里」)。原先「终端」(xterm 全屏 PTY)与「命令块」(Warp 式)是**两个独立导航入口**;现把命令块**搬进终端板块**统一为「一个板块、两种视图」。**做法(纯 Flutter,core/bridge 零改动)**:① 新增 `features/terminal/terminal_workspace.dart`——`TerminalWorkspace`(受控组件):顶部 `_ModeSwitcher` 分段切换器 `[ 终端 | 命令块 ]`(Midnight Ops token,选中 `surfaceActive`+accent),下方按 `blockMode` 渲染 `TerminalPanel`(传 `tabs`)或 `BlockTerminalPanel`(传 `blocks`+`connections`);② `app_scaffold`:**导航栏移除独立「命令块」项**(删 `_navBlocks=5` 常量、nav rail 第 6 项、`_center` 里的 `_navBlocks` 分支),终端分支(`_navTerminal`/默认)改渲染 `TerminalWorkspace`;视图模式 `bool _blockMode` 上提到 `AppScaffold`(`onModeChanged` 回调),这样 `_showMonitor` 收窄为 `_navTerminal && !_blockMode`——**传统终端带右侧监控栏、命令块整页占满**(与原独立页一致);`_openTerminal`(双击连接/卡片「终端」)顺手 `_blockMode=false` 切回传统视图。**关键点**:终端会话(`TerminalTabsController`)与命令块列表(`BlockTerminalController`)都由 `AppScaffold` 持有、与视图解耦,故**用条件渲染(非 IndexedStack)切换视图不丢状态**——切走时 `TerminalView` 卸载但 `TerminalSession` 仍在后台跑(PTY/订阅在控制器里),切回重挂只触发一次 idle resize(`_ptyId!=null` 不会重开 PTY,已核对 `terminal_session.dart` 护栏),命令块列表/cwd/历史同样留存。验收:`flutter analyze` 0 问题 / `flutter test` **42 通过** / `flutter build macos --debug` ✓ Built。**后续可选**:连接卡片加「命令块」直达(进终端板块 + 切命令块 + 设目标)。
- 2026-06-23:**Stage 9 落地——命令块终端原型(方案 A · Warp / 聊天流)**(用户选定方案 A「每条命令带自己的输出成一块、从上往下读、输入框钉底」,要 Flutter 桌面正式原型)。**与现有 xterm 全屏终端并存,不替换**,新增导航入口「命令块」。关键决策:① **执行模型 = 每条命令一个 exec 通道 + POSIX sentinel 包装**(`{ cd <cwd>; <cmd>; } 2>&1; __rsrc=$?; printf '\036%s\036%s\036' "$__rsrc" "$(pwd)"`):`2>&1` 合并 stderr(SSH `execute_command` 只捕获 stdout)、末尾 `printf` 恒退出 0 让 SSH/ADB 看到「成功」并原样回传、`$(pwd)` 反映用户 `cd` ⇒ **真实退出码 + cwd 跨块持久**都从哨兵解析;非 POSIX(Windows PowerShell)找不到哨兵 → `had_marker=false` 优雅降级(输出照显,无退出码/cwd)。② **core 几乎不动**:新增纯逻辑模块 `core/src/blockexec.rs`(`wrap_posix`/`parse_outcome`/`run_local`〔`$SHELL -c`,`tokio::process`〕)+ **11 个新单测**;**远程完全复用** `NativeConnectionManager::execute_command`(SSH/ADB 透明,零新增执行通道)。③ **bridge** `api/blockterm.rs`:`block_run_remote`(`ensure_session`→`execute_command(wrap_posix)`→`parse_outcome`)+ `block_run_local` + `BlockResultDto`,一次性请求-响应用 `Result`(非长流,无 frb unawaited 隐患);frb 重生成。④ **Flutter** `features/blockterm/`(组件化:`block_models`〔纯逻辑 + 单测〕/`block_repository`/`block_terminal_controller`〔目标 本机·连接 + cwd + 块列表 + 命令历史 + run〕/`block_terminal_panel` + `widgets/command_block_tile`〔状态字形 + `cwd ❯ cmd` + 耗时 + 退出码 chip + 复制/重跑 + 折叠〕/`widgets/block_input_bar`〔❯ 提示符 + 钉底输入 + 回车执行 + ↑/↓ 历史〕);自动滚底;走 Midnight Ops token。⑤ **本机模式**用 `tokio::process` 跑 `sh`,**无需任何服务器**即可在 Mac 试。状态延续轻量 `ChangeNotifier`(Riverpod 继续推迟)。验收:`cargo build --workspace` 零警告 / `cargo test` **50 通过**(原 39 + blockexec 11)/ `flutter analyze` 0 问题 / `flutter test` **42 通过**(原 38 + block_models 4)/ `flutter build macos --debug` ✓ Built。**真机端到端(本机 macOS)**:`echo`(27ms·绿 0)、`uname -sm && whoami`→`Darwin arm64 / shcodegojo`、**`cd /tmp` 后下一块提示符变 `/tmp`(cwd 持久生效)**、`cat /no-such-file-xyz`→红 `exit 1` + stderr「No such file or directory」全部正确。截图 `docs/gui/screenshots/{blockterm_local_demo,blockterm_empty}.png`。**注**:用 AppleScript 注入命令时撞上系统中文输入法把 `keystroke` 的英文吞成中文/丢空格,改用**剪贴板粘贴(Cmd+V)绕开 IME** 才稳。**原型边界**:暂不做流式输出(跑完才出块)、交互式/全屏程序(top/vim 仍走 xterm)、Windows PowerShell 哨兵;详见 [11-命令块终端原型](11-命令块终端原型.md) §2.4/§6。
- 2026-06-23:**终端板块移除传统终端、只留命令块(监控侧栏保留)**(用户:「在终端面板那里,把终端功能删了,留命令块,但是右边的监控还要」)。**做法(纯 Flutter,core/bridge 零改动)**:① `app_scaffold` 终端导航(`_navTerminal`)**直接渲染 `BlockTerminalPanel`**——原 `TerminalWorkspace` 的「终端 | 命令块」分段切换器整体退役;删除视图模式字段 `_blockMode` 与 `TerminalTabsController _tabs`(含 dispose);`_openTerminal`(双击连接 / 卡片「终端」)从「开 xterm PTY 标签」改为 **`_blocks.setTarget(c)`**(把命令块执行目标切到该连接)。② **监控侧栏保留**:`_showMonitor` 仍 `== _navTerminal`(终端板块旁永远带服务器监控、跟随选中连接),仅更新注释。③ **物理删除传统终端(xterm)整套 Dart**:`terminal_workspace`/`terminal_panel`/`terminal_session`/`terminal_tabs_controller`/`terminal_repository`/`terminal_theme`/`terminal_search`/`command_history` + `widgets/{terminal_toolbar,command_history_panel}`,连同孤儿测试 `terminal_search_test`/`command_history_test`;**`command_highlighter.dart` 因被命令块(`command_block_tile`/`block_input_bar`)复用,移入 `blockterm/`** 并改 3 处导入(含 `command_highlighter_test`),`features/terminal/` 目录整体移除。④ **移除 `xterm` 依赖**(`pubspec.yaml` + `flutter pub get`,连带退掉 `zmodem` 等传递依赖)。**注**:Rust bridge 的 `session_*`/`pty_*` 接口保留未删(已无 Dart 调用方,留给 CLI / 未来,零风险);命令块执行走 `block_run_remote/local`,与 PTY 通道无关。**验收**:`flutter analyze` **0 问题** / `flutter test` **28 通过**(原 48 − terminal_search 7 − command_history 13)/ `flutter build macos --debug` ✓ Built。
- 2026-06-23:**命令块多标签(按服务器)+ 侧栏去重设置图标 + 设置项真正生效(用户三连需求)**。**① 命令块改多标签、去掉面板内目标下拉**(用户:「终端板块不要自己选择,而是在什么标签页执行哪个服务器的命令」):新增 `block_session.dart` 的 `BlockSession`(单标签会话 = **构造时固定绑定一个目标**〔本机 / 某连接〕+ 块 / cwd / 历史 / `run`,目标不可变);`block_terminal_controller.dart` 重构为**多标签管理器**(`sessions` + `activeIndex` + `openFor(conn)`〔按连接 id 去重,已存在则聚焦〕/ `openLocal` / `setActive` / `closeAt`,构造默认开一个本机标签);`block_terminal_panel.dart` 改为「**标签条**(每标签 = 目标图标〔本机 computer / SSH dns / ADB android〕+ 名 + 关闭×,末尾 `+` 新开本机)+ 只读 **cwd 栏** + 活动标签块流 + 输入条」,**删除原 `_HeaderBar` 的「本机 ▾」目标下拉选择器**;`app_scaffold._openTerminal` 由 `setTarget` 改 `openFor`(双击侧栏连接 / 卡片「终端」= 开/聚焦该连接标签,不再临时切目标)。每标签 `BlockSession` 独立 `ChangeNotifier`,面板嵌套 `ListenableBuilder`(外层 controller 听标签增删/切换、内层 active session 听块/忙碌)。**② 侧边栏去重设置图标**(用户:「左侧有两个设置图标,只要一个」):`_sidebarFooter` 删掉与「添加连接」并排的齿轮,设置入口统一收敛到**左侧导航栏底部**那个;「添加连接」占满整行。**③ 设置真正生效 + 持久化**(用户:「完善设置的功能」):`flutter pub add shared_preferences`(2.5.5);`SettingsController` 加 `load()`(启动从磁盘读回)+ 每个 setter 落盘(`settings.*` 键);**真正接上三项**——(a) **命令块字号** `blockFontSize` → `BlockTerminalPanel.fontSize` → `CommandBlockTile`(命令头 + 输出)/ `BlockInputBar`(输入),**立即生效**;(b) **监控采样间隔** `MonitorController.intervalMs` 改为可变 + 新增 `setIntervalMs`(重建当前流),`app_scaffold._onSettingsChanged` 监听设置变更实时套用;(c) **MCP 随应用自启** `_initSettings` 在 `load()` 后若开启则 `mcp.start()`。**删除过时项「光标样式」**(传统终端已移除,无插入符概念);主题 / 强调色暂只**持久化偏好**(实时换肤需把 `AppColors` 静态常量整套改为可切换取色 = 大重构,本轮未做,UI 注「即将支持(偏好已保存)」)。`app_scaffold` initState:`load()` → 应用即时项 + `addListener` 监听后续变更(setState 重建套用字号 / 间隔)。验收:`flutter analyze` **0 问题** / `flutter test` **28 通过** / `flutter build macos --debug` ✓ Built;真机:终端板块顶部「本机 | Win-Dev (shanh)」双标签 + `+`、无目标下拉;设置页「命令块」字号 / 监控采样间隔 / MCP 自启可交互、已无光标样式。截图 `docs/gui/screenshots/{blockterm_tabs,settings_panel_v2}.png`(实拍)。**待续**:① 浅色 / 跟随系统**实时换肤**(取色重构);② 每标签**输入草稿保活**(当前切标签重置输入);③ `+` 也可直接选连接开标签(当前仅开本机,连接走侧栏)。
- 2026-06-23:**命令块支持 Windows / PowerShell(修复 Win 上命令块整条报 `Some(1)`)**(用户:「在 win 电脑上不能用命令吗?」)。**根因**:命令块远端执行用 `blockexec::wrap_posix` 把命令包成 POSIX 片段(`{ cd…; cmd; } 2>&1; __rsrc=$?; printf '\x1e退出码\x1e路径\x1e'`)来拿退出码 + 跨块 cwd;**Windows 默认 exec shell 是 PowerShell,不认这套语法 ⇒ 整条返回非零 ⇒ `execute_command` 报 `Command failed with code: Some(1)`**(监控能在 Win 跑是因为它发的是 PowerShell 脚本)。**修复**:① **core `blockexec.rs` 新增 `wrap_powershell(cwd, command)`**——等价 PowerShell 片段,打**同一套哨兵**(`[Console]::Out.Write($o+[char]30+$code+[char]30+(Get-Location).Path+[char]30)`),故 `parse_outcome` 通用;要点对齐 `monitor::windows_stats_command` 的可用写法:**只用单引号**(避开 Windows-OpenSSH exec 双引号 mangling)、`[Console]::OutputEncoding=UTF8`(防中文乱码)、`& {…} 2>&1 | Out-String -Stream -Width 512 | %{ $_.TrimEnd() } -join [char]10`(合并 stderr + 逐行去尾随空白避免补白膨胀)、退出码取 `$LASTEXITCODE`(原生)否则 `$?` 折 0/1(cmdlet)、`Set-Location`/`(Get-Location).Path` 管 cwd、末尾 `exit 0` 让 SSH 看到成功(真实码在哨兵)。② **bridge `api/blockterm.rs` 自动探测 + 缓存**:首次对某连接执行「先试 POSIX(命中哨兵→认定 POSIX)、未命中再试 PowerShell」,把 shell 种类按 connection_id 存进 `static Mutex<HashMap>`,后续直接用(POSIX 主机零额外开销,Windows 仅首条多一次往返);两种都没命中则优雅降级(原样输出)。**bridge 公共签名不变 → 无需 frb 重生成**。**真机端到端验证**(新增 `core/examples/block_win_smoke.rs` 走同套 `NativeConnectionManager` 直连 Win-Dev `shanh@192.168.0.112`):**① POSIX 包装 → `Err: Command failed with code: Some(1)`(复现原 bug);② PowerShell 包装 → `had_marker=true exit_code=0 cwd=C:\Users\shanh` + 正常目录列表**。**GUI 实测**:双击 Win-Dev 开标签 → 跑 `ls` → 绿对勾 · 942ms · 退出码 0 · 列出 `C:\Users\shanh` · 顶部 cwd 栏显示 `C:\Users\shanh`(截图 `docs/gui/screenshots/blockterm_windows_ls.png`)。验收:`cargo build --workspace` 零警告 / `cargo test -p r-shell-core` **54 通过**(+4 PowerShell 包装用例)/ `flutter build macos --debug` ✓ Built。**注**:`wrap_powershell` 假设 exec 默认 shell 为 PowerShell(与监控同前提);若为 cmd.exe 仍降级原样输出。
