# 进度 · conch

> 本项目进度明细在 **`docs/gui/进度.md`**(历史更全)。本文件仅作记忆系统的指针,避免两处分叉。

- 详见 [`docs/gui/进度.md`](gui/进度.md)。
- 最近(2026-08-10):修 iPhone「监控已断开 / Command failed Some(1)」——Darwin 采集无 python3 时改为纯 shell 回退(勿 exit 1);zsh 开启 `SH_WORD_SPLIT` 修好 Disk 0/0;Linux 误判需 `/proc` 证据。已覆盖 `~/.local/bin/r-shell` 与 `/Applications/Conch.app`。验收:`r-shell stats -c iPhone --insecure` → OS/内存/磁盘均有值、exit 0。
- 此前(2026-08-06):修 iOS MCP `ssh_session_open` 卡死(exec 超时 + reconnect 死锁 + iOS `elevate:su`→`sudo -S`);已装 `~/.local/bin/r-shell`。见 [`docs/gui/14-iOS-SSH卡死修复.md`](gui/14-iOS-SSH卡死修复.md)。
- 此前(2026-07-31):连接列表 2s 实时刷新;MCP `platform=ios|android|adb` 预设;Darwin 监控纠偏。见 [`docs/gui/13-连接实时刷新与MCP设备预设.md`](gui/13-连接实时刷新与MCP设备预设.md)。
